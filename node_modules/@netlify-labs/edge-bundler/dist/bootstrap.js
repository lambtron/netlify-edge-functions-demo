import { env } from 'process';
const BOOTSTRAP_LATEST = 'https://dinosaurs:are-the-future!@624accb72787800009364b4f--edge-bootstrap.netlify.app/index.ts';
const getBootstrapURL = () => { var _a; return (_a = env.NETLIFY_EDGE_BOOTSTRAP) !== null && _a !== void 0 ? _a : BOOTSTRAP_LATEST; };
export const getBootstrapImport = () => `import { boot } from "${getBootstrapURL()}";`;
